@model List<CategoryItemViewModel>

@if (Model?.Count > 0)
{
    foreach (var item in Model)
    {
        <a class="category-item" href="@item.Link" title="@item.DisplayName">@item.DisplayName</a>

    }
}
else
{
    <a class="category-item">No Data</a>
}
@model List<HotTagViewModel>

@if (Model?.Count > 0)
{
    foreach (var item in Model)
    {
        <a href="@item.TagUrl" rel="@item.TagCount" title="@item.TagName">@item.TagName</a>
    }
}
else
{
    <a href="javascript:void(0);" rel="0" title="Tag Empty">Tag Empty</a>
}